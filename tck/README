1. To run TCK tests with current API to check against existing signatures:

   mvn test
   
2. To generate new signatures

  a. Modify property: signature-api.version for API version you're generating
  
  b. Create new folder src/signatures/${signature-api.version} (if it's not yet there)

  c. run:
   mvn install -P generate-api-signature  ( this will generate signature for current JDK, defined by JAVA_HOME)
   
   You might have to run again with different JAVA_HOME for 1.7 or other
   
   ie:
   
   JAVA_HOME=/path/to/jdk1.7  mvn install -P generate-api-signature
